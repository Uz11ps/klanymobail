VPS_IP=89.104.68.191
VPS_USER=root
VPS_PASSWORD=CHANGE_ME

# Remote folder where releases/current live
APP_DIR=/opt/klany

# Local files (relative to repo root)
ENV_FILE=.env.server
COMPOSE_FILE=docker-compose.yml

# How many releases to keep on server
KEEP_RELEASES=5

# Set to 1 to deploy even if git status is dirty
# ALLOW_DIRTY=1


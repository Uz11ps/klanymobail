# Included from conf.d inside http{}. Loaded before default.conf (00 < default).
map $http_access_control_request_headers $storage_cors_allow_headers {
    ""      "*";
    default $http_access_control_request_headers;
}
